# Storage. If S3_BUCKET and AWS credentials are set the app writes to S3,
# otherwise it falls back to LOCAL_STORAGE_DIR on the filesystem.

LOCAL_STORAGE_DIR=./data

# S3_BUCKET=your-bucket-name
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=ap-south-1